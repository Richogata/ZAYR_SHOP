# PostgreSQL (Supabase / Vercel Postgres) — optionnel en local (fichier JSON utilisé sinon)
DATABASE_URL="postgresql://user:password@host:5432/zayr_shop?schema=public"

# Administration (/admin)
ADMIN_PASSWORD="changez-moi"
ADMIN_SECRET="secret-token-pour-api-admin"
